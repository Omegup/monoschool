import { createUseStyles } from "react-jss";

export const useAvatarWithNameStyles = createUseStyles({
  container: {
    display: 'flex',
    paddingInline: 10,
    gap: 10,
    alignItems: 'center'
  }
})