import { createUseStyles } from "react-jss";

export const useTextWithIconStyles = createUseStyles({
  container: {
    display: 'flex',
    paddingInline: 10,
    gap: 10,
    alignItems: 'center'
  }
})