import { checkboxVariant, checkboxSize } from './CheckBox.types';

export const DEFAULT_CHECKBOX_VARIANT: checkboxVariant = 'border' as const;
export const DEFAULT_CHECKBOX_SIZE: checkboxSize = 'medium' as const;
